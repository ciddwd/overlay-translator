<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Keep the main configuration's HTTP policy; PrivateCleartextInterceptor enforces it. -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Debug resource only, additionally gated by android:debuggable. Never trust all TLS peers. -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
